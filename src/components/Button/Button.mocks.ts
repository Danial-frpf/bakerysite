import { IButton } from ".";

const small: IButton = {
    label: "Small",
    size: "sm",
};

const medium: IButton = {
    label: "Medium",
    size: "md",
};

const large: IButton = {
    label: "Large",
    size: "lg",
};

export const mockButtonProps = {
    small,
    medium,
    large,
};
